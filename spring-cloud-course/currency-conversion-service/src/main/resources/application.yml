spring:
  application:
    name: currency-conversion-service

server:
  port: 8100

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/eureka

# currency-exchange-service:
#  ribbon:
#    listOfServers: http://localhost:8000, http://localhost:8001, http://localhost:8002